# Application
APP_NAME=FinSight
APP_ENV=development
DEBUG=true

# API
API_HOST=0.0.0.0
API_PORT=8000

# CORS — comma-separated origins allowed to call the API from the browser
CORS_ORIGINS=http://localhost:3000

# Database (psycopg v3 driver)
DATABASE_URL=postgresql+psycopg://gigiguan@localhost:5432/finsight
